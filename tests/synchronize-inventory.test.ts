import{expect,it}from"vitest";import{SynchronizeInventory}from"../src/workflows/synchronize-inventory.js";
it("previews mapped inventory without writing",async()=>{const writes:unknown[]=[];const source={readWarehouse:async()=>[{sku:"BAN-1",traceable_available:2,non_traceable_available:1,default_conversion:2,non_traceable_conversion:1}]};const target={readAtLocation:async()=>new Map([["BAN-1",{sku:"BAN-1",inventory_item_id:"1",tracked:true,available:3}]]),setAvailable:async(...args:unknown[])=>{writes.push(args)}};const result=await new SynchronizeInventory(source,target).run("gid://location/1",2,true,10);expect(result.summary.would_update).toBe(1);expect(result.items[0]?.to).toBe(5);expect(writes).toHaveLength(0)});
